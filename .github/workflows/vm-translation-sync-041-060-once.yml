name: Vandikkaran Magan English sync 41-60 once
on:
  push:
    branches: [main]
    paths: ['.github/workflows/vm-translation-sync-041-060-once.yml']
permissions:
  contents: write
jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Synchronize English checkpoint
        run: python works/vandikkaran-magan/scripts/sync_translation_batch_041_060.py
      - name: Check repository diff
        run: git diff --check
      - name: Commit synchronized checkpoint
        run: |
          rm -f .github/workflows/vm-translation-sync-041-060-once.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git diff --cached --check
          git commit -m 'Synchronize Vandikkaran Magan English scenes 41-60 [skip ci]'
          git push origin main
