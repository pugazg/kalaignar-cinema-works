# வண்டிக்காரன் மகன் — historical Tamil glyph audit

Guide: `docs/HISTORICAL_TAMIL_GLYPH_TRANSCRIPTION_GUIDE.md`

Known mandatory families:

`ணா / ணை / ணொ / ணோ / லை / ளை / றா / றொ / றோ / னா / னை / னொ / னோ`

## Prospective first-pass coverage

| PDF | Printed | First-pass glyph check | Representative direct-scan evidence | Final glyph verification |
|---:|:---:|---|---|---|
| 4 | — | PASS for first-pass drafting | complete page inspected; no global substitution | PASS — dedicated batch 1 |
| 5 | — | PASS for first-pass drafting | `அண்ணா` → historical `ணா` identity decoded in Unicode | PASS — dedicated batch 1 |
| 6 | 5 | PASS for first-pass drafting | complete page inspected; source labels/punctuation retained | PASS — dedicated batch 1 |
| 7 | 6 | PASS for first-pass drafting | earlier prospective note cited `நாணயத்தின்` as `ணா`; dedicated reread confirms that word is not a `ணா` occurrence. `என்னால்` supplies a valid `னா` occurrence | PASS — dedicated batch 1 |
| 8 | 7 | PASS for first-pass drafting | complete page inspected; no global substitution | PASS — dedicated batch 1 |

The prospective drafting checks above did not themselves satisfy the dedicated gate. PDF 4–8 has now been independently re-read under the dedicated historical-glyph phase.

## Dedicated historical-glyph verification

### Batch 1 — PDF 4–8 — PASS / 5 of 87

All occurrences of the mandated families on these five pages were re-read directly from the controlling scan. Glyph identity was decoded before checking the Unicode transcription; surrounding source spelling, grammar, punctuation, and layout were left untouched.

| PDF | Printed | Target-family evidence re-read from source | Result |
|---:|:---:|---|---|
| 4 | — | `எதிர்ப்புகளை`, `இழிவுகளைத்`, `பிள்ளையாய்ப்`, `பிள்ளைதான்`, `சண்டமாருதங்களையும்` (`ளை`); `நூற்றாண்டு` (`றா`) | PASS — no glyph correction |
| 5 | — | `அண்ணா` (`ணா`, multiple source occurrences); `எதிரிகளைப்` (`ளை`); `தலையெழுத்தை`, `இல்லையேல்` (`லை`); `மகனை` (`னை`) | PASS — no glyph correction |
| 6 | 5 | full page re-read; no mandated-family occurrence requiring a glyph-identity correction | PASS — no glyph correction |
| 7 | 6 | `காலைத்தொட்டுக்`, `கவலைப்படாதே`, `எல்லைக்குள்`, `தலையைக்` (`லை`); `கட்டளை`, `தர்மங்களையெல்லாம்`, `புலவர்களையும்` (`ளை`); `என்னால்`, `குமணனாய்`, `கர்ணனாய்க்` (`னா`); `புருஷனைத்` (`னை`) | PASS — no glyph correction |
| 8 | 7 | `மனைவி` (`னை`); `கவலை` (`லை`); `விளையாடு` (`ளை`) | PASS — no glyph correction |

### Batch 1 disposition

- dedicated pages verified: **5 / 87**;
- contiguous dedicated range: **PDF 4–8**;
- source-supported glyph-identity corrections to canonical page text: **0**;
- audit-only correction: the old prospective `நாணயத்தின்` → `ணா` example is withdrawn because `நாணயத்தின்` does not contain the `ணா` family;
- unresolved glyph holds: **0**;
- next dedicated batch: **PDF 9–13**.

### Batch 2 — PDF 9–13 — PASS / 10 of 87

Every occurrence of the mandated historical-glyph families on PDF 9–13 was independently re-read from enlarged controlling-source pixels. The historical shapes consistently resolve to the Unicode identities already present in the visual-verified transcription. No ordinary wording, punctuation, scene structure, or source spelling was reopened.

| PDF | Printed | Target-family evidence re-read from source | Result |
|---:|:---:|---|---|
| 9 | 8 | `முன்னால்` (`னா`); `வளைத்து` (`ளை`); `சிலை`, `சிலையை` (`லை`); `வேலைகளைப்` (`லை / ளை`); `வெளியேறுகிறான்` (`றா`) | PASS — no glyph correction |
| 10 | 9 | `கட்டிலை` (`லை`); `கருணை` (`ணை`); `இன்னைக்கு` (`னை`); `நுழைகிறார்`, `படுத்திருக்கிறாள்`, `தருகிறாள்` (`றா`) | PASS — no glyph correction |
| 11 | 10 | `இணைந்து`, `தலையணைக்குக்` (`ணை`); `கண்ணாளா`, `மூணாவது` (`ணா`); `இருக்கிறோம்` (`றோ`); `அவளைத்`, `விளையாடிய` (`ளை`); `உன்னை` (`னை`) | PASS — no glyph correction |
| 12 | 11 | `என்னை` (`னை`); `போகிறோம்` (`றோ`); `நிலைதடுமாறி`, `காலையும்` (`லை`); `சுக்குநூறாப்` (`றா`); `அனாதையா`, `அனாதை` (`னா`) | PASS — no glyph correction |
| 13 | 12 | `பானை` (`னை`); `நாளைக்கு`, `விளையாடலாம்` (`ளை`); `சொன்னா` (`னா`); `மற்றொரு` (`றொ`); `கண்ணாமூச்சி` (`ணா`); `விளையாடுகிறார்கள்` (`ளை / றா`) | PASS — no glyph correction |

### Batch 2 disposition

- dedicated pages verified: **10 / 87**;
- contiguous dedicated range: **PDF 4–13**;
- source-supported glyph-identity corrections to canonical page text in this batch: **0**;
- cumulative canonical glyph corrections in the dedicated phase: **0**;
- unresolved glyph holds: **0**;
- next dedicated batch: **PDF 14–18**.

### Batch 3 — PDF 14–18 — PASS / 15 of 87

Every occurrence of the mandated historical-glyph families on PDF 14–18 was independently re-read from enlarged controlling-source pixels. The source shapes resolve to the Unicode identities already present in the visual-verified transcription. No ordinary spelling, punctuation, scene structure, or performance layout was reopened.

| PDF | Printed | Target-family evidence re-read from source | Result |
|---:|:---:|---|---|
| 14 | 13 | `மாப்பிள்ளை`, `பிள்ளைங்க` (`ளை`); `கல்லை`, `நிழலை`, `நிழலைக்கூட`, `விரலையும்` (`லை`); `அவனைத்`, `அவனைக்` (`னை`) | PASS — no glyph correction |
| 15 | 14 | `குழந்தைகளை` (`ளை`); `தலைமயிரைப்`, `தலைமுடியைப்`, `மயிலை` (`லை`); `அவளை`, `காளை`, `அவளைத்` (`ளை`) | PASS — no glyph correction |
| 16 | 15 | `கவலைப்படாதே`, `தலையை` (`லை`); `உன்னைக்`, `உன்னை` (`னை`); `சொன்னாய்` (`னா`) | PASS — no glyph correction |
| 17 | 16 | `கண்ணாடியை` (`ணா`); repeated `லீனா` (`னா`); `அவளைப்` (`ளை`) | PASS — no glyph correction |
| 18 | 17 | `சரியில்லை` (`லை`); `எத்தனை` (`னை`) | PASS — no glyph correction |

### Batch 3 disposition

- dedicated pages verified: **15 / 87**;
- contiguous dedicated range: **PDF 4–18**;
- source-supported glyph-identity corrections to canonical page text in this batch: **0**;
- cumulative canonical glyph corrections in the dedicated phase: **0**;
- unresolved glyph holds: **0**;
- next dedicated batch: **PDF 19–23**.

### Batch 4 — PDF 19–23 — PASS / 20 of 87

Every occurrence of the mandated historical-glyph families on PDF 19–23 was independently re-read from enlarged controlling-source pixels. The source shapes resolve to the Unicode identities already present in the visual-verified transcription. No ordinary wording, punctuation, scene structure, or source spelling was reopened.

| PDF | Printed | Target-family evidence re-read from source | Result |
|---:|:---:|---|---|
| 19 | 18 | `போறவங்களையெல்லாம்`, `இளைய` (`ளை`); `இல்லை`, `அலையுற` (`லை`); `போனா` (`னா`) | PASS — no glyph correction |
| 20 | 19 | `எவனாயிருந்தாலும்`, `பண்ணினார்` (`னா`) | PASS — no glyph correction |
| 21 | 20 | `லிங்கனை` (`னை`); full page re-read for the remaining mandated families | PASS — no glyph correction |
| 22 | 21 | `தலையை` (`லை`); `சென்றாரு`, `கொன்றாரு` (`றா`) | PASS — no glyph correction |
| 23 | 22 | `உன்னைக்` (`னை`); `பெண்ணா` (`ணா`); `இளையராணி` (`ளை`); `மாலை`, `காலையில` (`லை`) | PASS — no glyph correction |

### Batch 4 disposition

- dedicated pages verified: **20 / 87**;
- contiguous dedicated range: **PDF 4–23**;
- source-supported glyph-identity corrections to canonical page text in this batch: **0**;
- cumulative canonical glyph corrections in the dedicated phase: **0**;
- unresolved glyph holds: **0**;
- next dedicated batch: **PDF 24–28**.

### Batch 5 — PDF 24–33 — PASS / 30 of 87

Per the user's revised cadence, the dedicated historical-glyph phase now proceeds in **10-page iterations**. Every occurrence of the mandated historical-glyph families on PDF 24–33 was re-read against enlarged controlling-source pixels. The source shapes resolve to the Unicode identities already present in the visual-verified transcription. No ordinary wording, punctuation, scene structure, or performance layout was reopened.

| PDF | Printed | Target-family evidence re-read from source | Result |
|---:|:---:|---|---|
| 24 | 23 | `காலையிலே`, `பரவாயில்லை` (`லை`); `மனுஷனைக்` (`னை`) | PASS — no glyph correction |
| 25 | 24 | `வயசானவனு` (`னா`); `கண்ணாயிரம்` (`ணா`); `கண்ணைத்`, `கண்ணைத்திறந்து` (`ணை`) | PASS — no glyph correction |
| 26 | 25 | `கண்ணாயிரம்` (`ணா`); `என்னைப்பன்` (`னை`); `கண்ணை` (`ணை`) | PASS — no glyph correction |
| 27 | 26 | `கண்ணாயிரம்` (`ணா`); `தலைமயிரைப்` (`லை`); `என்னையா`, `உன்னை`, `அவனைத்தடுத்து`, `காளிங்கனைப்` (`னை`) | PASS — no glyph correction |
| 28 | 27 | `வயசான` (`னா`); complete page re-read for remaining target families | PASS — no glyph correction |
| 29 | 28 | `காலை` (`லை`); `குதிரைகளைப்` (`ளை`) | PASS — no glyph correction |
| 30 | 29 | `உன்னையும்` (`னை`); `இன்னொரு` (`னொ`) | PASS — no glyph correction |
| 31 | 30 | `இன்னொரு` (`னொ`); `நொண்டியபடியே` (`ணொ`); `இன்னைக்கு` (`னை`) | PASS — no glyph correction |
| 32 | 31 | `அவனைப்பிடிச்சு`, `குடிகாரனை`, `உன்னை`, `தகப்பனை` (`னை`); `பிள்ளை`, `பிள்ளைமேலயா`, `பிள்ளையோ` (`ளை`) | PASS — no glyph correction |
| 33 | 32 | repeated `பச்சிலைச்சாறு / பச்சிலையைப் / பச்சிலை` (`லை`) | PASS — no glyph correction |

### Batch 5 disposition

- dedicated pages verified: **30 / 87**;
- contiguous dedicated range: **PDF 4–33**;
- source-supported glyph-identity corrections to canonical page text in this batch: **0**;
- cumulative canonical glyph corrections in the dedicated phase: **0**;
- unresolved glyph holds: **0**;
- dedicated batch size from this point forward: **10 source pages per iteration**;
- next dedicated batch: **PDF 34–43**.

### Batch 6 — PDF 34–48 — PASS / 45 of 87

Per the user's revised cadence, the dedicated historical-glyph phase now proceeds in **15-page iterations**. Every occurrence of the mandated historical-glyph families on PDF 34–48 was independently re-read against enlarged controlling-source pixels. The historical shapes resolve to the Unicode identities already present in the visual-verified transcription. Ordinary wording, punctuation, scene structure, and song/performance layout were not reopened.

| PDF | Printed | Target-family evidence re-read from source | Result |
|---:|:---:|---|---|
| 34 | 33 | `காளிங்கராயனைக்`, `அவனைத்` (`னை`) | PASS — no glyph correction |
| 35 | 34 | `கலைச்சிட்டே`, `அலைகள்`, `தலைப்பாகத்தைப்` (`லை`); `விங்கனை` (`னை`); `பிள்ளையாரப்பா` (`ளை`) | PASS — no glyph correction |
| 36 | 35 | `விளையாட்டே` (`ளை`); repeated `இல்லை`, `மாலை`, `சேலை`, `வேலை` (`லை`); `முத்தாடினான்`, `கொண்டாடினாள்`, `நீராடினாள்` (`னா`) | PASS — no glyph correction |
| 37 | 36 | `விளையாடக்` (`ளை`); `விங்கனா` (`னா`) | PASS — no glyph correction |
| 38 | 37 | `மனநிலையை` (`லை`); `பானையிலே` (`னை`) | PASS — no glyph correction |
| 39 | 38 | `அவைகளைப்` (`ளை`); `இன்னைக்கு` (`னை`); `நாளைக்கு` (`ளை`) | PASS — no glyph correction |
| 40 | 39 | `விளையாட்டு` (`ளை`); `பிடிக்கவேணா` (`ணா`) | PASS — no glyph correction |
| 41 | 40 | repeated `என்னை` (`னை`); `காலையில` (`லை`) | PASS — no glyph correction |
| 42 | 41 | `மலைப்பங்களா` (`லை`); `நுழைகிறான்` (`றா`) | PASS — no glyph correction |
| 43 | 42 | `பாவனை` (`னை`); `தலையணையையும்` (`ணை`); `கட்டிலை` (`லை`); repeated `யானை` (`னை`) | PASS — no glyph correction |
| 44 | 43 | `மிருகங்களைப்` (`ளை`); `சேலைத்`, `கவலைப்படுறே`, `மலைப்பங்களா` (`லை`); `யானை` (`னை`); `ஆணை` (`ணை`) | PASS — no glyph correction |
| 45 | 44 | `கண்ணைத்` (`ணை`); `தலைகீழாத்தான்` (`லை`); `தவளை` (`ளை`); `பஜனை`, `அவனைப்` (`னை`) | PASS — no glyph correction |
| 46 | 45 | `பருவச்சீலை`, repeated `இல்லை`, `சேலை`, `வேலை` (`லை`); `முத்தாடினான்`, `கொண்டாடினாள்`, `நீராடினாள்` (`னா`) | PASS — no glyph correction |
| 47 | 46 | `வீணை` (`ணை`); `தாலாட்டினாள்`, `பாராட்டினாள்` (`னா`) | PASS — no glyph correction |
| 48 | 47 | `என்னை`, `பேரனையோ`, `அப்பனை`, `அன்னைக்கு` (`னை`); `கவலைப்படாதே`, `மாலை` (`லை`); `பிள்ளையும்` (`ளை`); `அணிவிக்கிறான்` (`றா`); `ஆனானோ` (`னா / னோ`) | PASS — no glyph correction |

### Batch 6 disposition

- dedicated pages verified: **45 / 87**;
- contiguous dedicated range: **PDF 4–48**;
- source-supported glyph-identity corrections to canonical page text in this batch: **0**;
- cumulative canonical glyph corrections in the dedicated phase: **0**;
- unresolved glyph holds: **0**;
- dedicated batch size from this point forward: **15 source pages per iteration**;
- next dedicated batch: **PDF 49–63**.
