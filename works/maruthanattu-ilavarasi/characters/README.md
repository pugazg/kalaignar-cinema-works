# மருதநாட்டு இளவரசி — character/entity layer

Status: **COMPLETE-VERIFIED**

Authority: `../dialogues/index.json` — **208 immutable dialogue records / QA PASS**.

## Coverage

- exact source speaker labels: **24/24 mapped**;
- dialogue records: **208/208 mapped exactly once**;
- entity records: **14**;
- named characters: **11**;
- generic role categories: **1**;
- collectives: **2**;
- unresolved labels / records: **0 / 0**;
- dialogue records modified: **0**.

Exact source labels remain immutable provenance. Abbreviations and spelling variants are reconciled only in this separate layer.

Mapped alias groups include:

- `கோபதிவர்மன்` / `கோபதி`;
- `அன்பானந்தர்` / `அன்பா`;
- `துர்ஜயன்` / `துர்ஜ` / `துர்ஜய`;
- `காளிங்கன்` / `காளிங்`;
- `மணிபல்லவர்` / `மணி`;
- `ரௌத்ரன்` / `ரௌத்` / `ரௌத்திரன்`;
- `காண்டிபன்` / `காண்` / `காண்டி`.

`சேவகன்` remains an unnamed role. `சபையோர்` and `பொதுமக்கள்` remain collective source entities. No unsupported personal names are introduced.

## QA

See `../notes/character-entity-qa.json` and `../notes/character-entity-qa.md`.

## Next activity

Begin Phase 8 song/performance/authorship gating. Inventory source-visible performance or song structures, if any; preserve exact wording/lineation/role cues/credits and do not infer authorship from inclusion in the booklet. Run whole-work performance coverage QA before English translation.
